function [outputArg1,outputArg2] = map_across_sessions(session_data,inputArg2)
%Map Across Sessions: Map function func across sessions
%   session_data: cell array where session_data{i
outputArg1 = inputArg1;
outputArg2 = inputArg2;
end

